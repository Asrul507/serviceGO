{
  "name": "servicego",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:init": "wrangler d1 execute servicego_db --local --file=./schema.sql",
    "d1:init:remote": "wrangler d1 execute servicego_db --remote --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240405.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.50.0"
  },
  "dependencies": {
    "hono": "^4.2.0"
  }
}
